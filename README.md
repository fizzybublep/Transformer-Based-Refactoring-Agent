# Transformer-Based-Refactoring-Agent

Setup a virtual environment and add packages according to the requirements.txt
The dataset_generation.ipynb works on the two files of smell-affects and DetailedDeps folder
The DetailedDeps folder has dot files of class level dependencies obtained from tika using Maven
